// Object is stored in key an values 
// Array is stored in a ordered collection 

const Object = {
    name:"Sachin"
}
const Array = ["Sachin"]
Array.push("ss") // add at last 
Array.pop() // delete the last element

console.log(Object.name)

console.log(Array[0],Array[1])